17-10-2025 15:19

Tags: [[C]]

# Custom Types

In order to create a custom alias for a type, we use the `typedef` keyword as follows:

```C
typedef long long unsigned int my_int;
```

Here we have created a `typedef` that represents `long long unsigned int`, its literally the same but can be written much shorter and has the exact same effect.




