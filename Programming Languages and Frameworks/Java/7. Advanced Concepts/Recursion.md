17-08-2025 13:42

Tags: [[Java]]

# Recursion

## StackOverflowError

A `StackOverflowError` happens when the memory allocated for a program's call stack has been completely used up. This happens when an infinite recursion occurs because each method call consumes a piece of memory without releasing it.




